import { CommunityView } from '@/features/user/CommunityView';

export const metadata = {
  title: 'Looks | SLOOK',
  description: 'Explore signature styles from the SLOOK community.',
};

export default function LooksPage() {
  return <CommunityView />;
}
