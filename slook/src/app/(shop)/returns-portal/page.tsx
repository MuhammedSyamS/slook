import { ReturnPortalView } from '@/features/order/ReturnPortalView';

export const metadata = {
  title: 'Returns Portal | SLOOK',
  description: 'Initiate a return or exchange for your SLOOK orders.',
};

export default function ReturnPortalPage() {
  return <ReturnPortalView />;
}
