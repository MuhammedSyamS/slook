import { ContactView } from '@/features/contact/ContactView';

export const metadata = {
  title: 'Contact Us | SLOOK',
  description: 'Establish communication with SLOOK Studio for support and inquiries.',
};

export default function ContactPage() {
  return <ContactView />;
}
