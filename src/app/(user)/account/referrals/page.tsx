import { ReferralsView } from '@/features/user/ReferralsView';

export const metadata = {
  title: 'Referrals | SLOOK',
  description: 'Invite friends to SLOOK and earn store credits.',
};

export default function ReferralsPage() {
  return <ReferralsView />;
}
